basic multiyear test
BUG !!! DoY MUST be combined with year when aggregating


df_daily_na
	rename to mean Aa


Было бы еще хорошо вывести суточные ходы в виде отдельного численного аутпута, еще одну csv-шку, сверху вниз от 0:00 до 23:30, переменная_месяц, для каждой переменной 12 колонок будет.


А тепловые карты незаполненные+заполненные можно скачать как одну картинку для каждой переменной? В аутпутах их не было?
У меня как-то по отдельности скачиваются и не стандартным инструментом (фотоаппарат), а сохранить как

Стандртный инструмент сохранить картинку как png
Может, только для интерактивных действует

Большие расстояния между графиками-заголовками-графиками. Сжать бы их.
Тут еще помимо больших расстояний "2023" нужно просто вместить в ту же строчку после (Wm-2), не нужна там отдельная строчка

Колаб кстати, вообще почему-то выводит не самый первый график в поле экрана, а последний, потом надо около графика мышкой встать и вверх крутить, чтобы тепловые карты показались. Не знаю, лечится это или нет.

Git action который автоматически генерирует compare?

1) Добавить описание, похожее на на конфиг загрузки


1)	нужно ли усреднять отдельно по годам? 
	т.е. если данные 2003 - 2005, что делать
	Картинки вроде отдельно
-> 1) Да, отдельно по годам
	
	Сейчас - все пойдут в один файл

2) 6 в смысле 6-й мес оставить, а NA вместо значений
