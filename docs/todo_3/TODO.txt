+ Было бы еще хорошо вывести суточные ходы в виде отдельного численного аутпута, 
еще одну csv-шку, сверху вниз от 0:00 до 23:30, переменная_месяц, для каждой переменной 12 колонок будет.

Может, лучше в разных файлах каждую переменную. 
Название файла NEE_courses и в файле табличку сверху вниз от 00:00 до 23:30, 
каждый новый столбец вправо - новый месяц.
csv-файл с суточными ходами, еще один аутпут

+ write that ustar_f cols dont exist

+ ERpoc adds GMT to time if file is 3 mon short? that's the bug?
	why Reco was required?
	why not equal on load?
	why attributes changed?
	why GMT suddenly there? EProc?

+ REddyProc в некоторых случаях сбоит на ustarFiltering, тогда: 
	можно параметр попытаться пропатчить на ручной параметр
	можно параметр совсем слепо выбрать по типу экосистемы трава \ лес
	дальше его нужно применить каким-то способомв какой-то момент: 
		либо до REddyProc
		либо пропатчить  REddyProc полноценно в либе или во время одной из функций
	само применение фильтра - 
		см пример в Excel

+ Telegram QA
+ Pack soures file in archive too

>>>>> MAYBE

+ pdf and copy text also with png
+ настроить сравнивалку для блокнотов exe, SO answer, другим

+ Commit to RProc fixes? or share notebook
	- random 2 last column output 1.3.3?!
	- output size not wrapped in the end
	- stdout and stderr are mixed
	- auto ustar fallback option

>>>>> LATER

Git action который автоматически генерирует compare?